[project]
name = "polymarket-whale-backend"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = [
  "fastapi>=0.110",
  "uvicorn[standard]>=0.23",
  "httpx>=0.26",
  "pydantic>=2.6",
  "pydantic-settings>=2.2",
]

[tool.uvicorn]
factory = false